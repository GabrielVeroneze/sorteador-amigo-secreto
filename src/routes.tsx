import { BrowserRouter as Router, Route, Routes } from 'react-router-dom'

const AppRoutes = () => {
    return (
        <Router>
            <Routes>

            </Routes>
        </Router>
    )
}

export default AppRoutes
